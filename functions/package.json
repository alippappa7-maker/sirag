{
  "name": "siraj-copilot-functions",
  "version": "1.0.0",
  "description": "سراج — المساعد الإسلامي الذكي Cloud Functions",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "private": true
}
