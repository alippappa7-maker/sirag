# قائمة التحقق للنشر والإطلاق (Release Checklist) - سراج

## 1. الفحوصات الفنية وتجهيز البناء (Build & Technical Readiness)
- [x] **تحديث أرقام الإصدار**: ضبط `versionCode: 1` و `versionName: "1.0.0"` في `app/build.gradle.kts`.
- [x] **تفعيل R8/ProGuard**: تفعيل `isMinifyEnabled = true` و `isShrinkResources = true` لحزمة الإنتاج (Release).
- [x] **توقيع الحزمة (App Signing)**: تجهيز مفاتيح التوقيع وحفظ ملف `keystore` بأمان واستخدام `Play App Signing`.
- [x] **حزم النشر (App Bundles)**: توليد ملفات `.aab` لأندرويد و `.ipa` للـ iOS عبر بيئة البناء الرسمية.
- [x] **فحص الانهيارات والذاكرة**: التأكد من خلو البناء من تسريبات الذاكرة ومعالجة مشكلة `ComponentActivity`.
- [x] **التشغيل دون اتصال (Offline Mode)**: التحقق من عمل واجهات المحراب والمشاريع المحفوظة بدون إنترنت.

---

## 2. إعدادات الخادم والخدمات السحابية (Backend & Cloud Configuration)
- [x] **قواعد أمان Firestore & Storage**: نشر القواعد المقفلة وتأكيد منع العميل من تعديل الأرصدة أو الصلاحيات.
- [x] **مفاتيح وأسرار الخدمات**: تهيئة مفاتيح `GEMINI_API_KEY` و `App Store` داخل `Google Cloud Secret Manager`.
- [x] **فهارس قاعدة البيانات (Composite Indexes)**: إنشاء الفهارس المطلوبة لاستعلامات الومضات وطابور المراجعة والمشاريع.
- [x] **تفعيل حماية التطبيق (Firebase App Check)**: ربط `PlayIntegrity` لرفض الطلبات غير الشرعية.
- [x] **حدود الاستهلاك والتكلفة (Budget Alerts & Quotas)**: تعيين تنبيهات السحابة عند 50% و 80% و 100% مع مفتاح تعطيل طارئ.

---

## 3. مراجعة المتطلبات القانونية والمتاجر (Store & Legal Compliance)
- [x] **سياسة الخصوصية (Privacy Policy)**: توفير رابط معتمد ومطابق لسياسات Google Play و Apple App Store.
- [x] **شروط الاستخدام (Terms of Service)**: إبراز شروط عدم استخدام التطبيق في محتوى غير لائق أو مخالف للشرع.
- [x] **نموذج أمان البيانات (Data Safety / Privacy Nutrition Labels)**: الإفصاح عن البيانات المجمعة (البريد، سجل الأعطال، المشتريات).
- [x] **آلية حذف الحساب (Account Deletion Flow)**: توفير ميزة الحذف المباشر من داخل التطبيق مع فترة سماح 14 يوماً.
- [x] **الإبلاغ عن المحتوى والحظر (UGC Moderation & Reporting)**: توفير خيارات إبلاغ فورية عن أي ومضة أو محتوى مخالف.
- [x] **الإفصاح عن الذكاء الاصطناعي (AI Generation Disclaimers)**: وسم كل وسائط مولدة بالذكاء الاصطناعي بوضوح تام.

---

## 4. الجاهزية الشرعية والتحريرية (Sharia & Content Readiness)
- [x] **قواعد بيانات المصادر الموثوقة**: مطابقة نصوص القرآن الكريم برسم المصحف والتأكد من عدم توليد آيات أو أحاديث بالذكاء الاصطناعي.
- [x] **حسابات المراجعين المعتمدين**: تعيين وتفعيل حسابات المراجعين الشرعيين بصلاحية `REVIEWER` في بيئة الإنتاج.
- [x] **حظر التجسيد والتمثيل المحظور**: التحقق من فلاتر الخادم لمنع أي طلبات لتجسيد الأنبياء أو الصحابة.
- [x] **المحتوى التأسيسي المعتمد (Seed Content)**: تجهيز باقة من الومضات والقوالب الدعوية المعتمدة رسمياً لتكون جاهزة للمستخدمين الجدد.

---

## 5. المراقبة وخطط الطوارئ (Monitoring & Incident Response)
- [x] **تفعيل Firebase Crashlytics**: تتبع الأخطاء الفورية مع حجب أي بيانات شخصية أو أسرار من التقارير.
- [x] **سجلات التدقيق الإداري (Audit Logs)**: تسجيل كل قرار اعتماد أو رفض أو حذف بشكل غير قابل للتعديل.
- [x] **خطة التراجع (Rollback Strategy)**: توفير إمكانية تعطيل ميزة التوليد بالذكاء الاصطناعي فوراً عبر Firebase Remote Config / Secret Manager دون الحاجة لتحديث التطبيق.

---

## 6. إجراءات يوم الإطلاق (Launch Day Protocol)
1. رفع حزمة الإنتاج `1.0.0` إلى مسار الاختبار المغلق (Closed Track) والتحقق من المراجعة التلقائية للمتاجر.
2. تفعيل المراقبة الحية للـ Crashlytics واستهلاك الـ Cloud Functions.
3. التوسع التدريجي في النشر (10% -> 25% -> 50% -> 100%).
4. متابعة تقييمات المستخدمين وملاحظات المراجعين في أول 48 ساعة.
