# القيود المعروفة (Known Limitations) - إصدار 1.0.0 (MVP)

تسجل هذه الوثيقة القيود الفنية والتشغيلية في الإصدار الأول من تطبيق سراج، والتي من المقرر معالجتها في الإصدارات المستقبلية.

## 1. قيود المنصات (Platforms)
- **نسخة iOS:** التطبيق يعتمد على Kotlin Multiplatform (KMP/CMP)، لكن بناء وتشغيل نسخة iOS معلّق (Pending) حتى تتوفر بيئة Xcode / macOS للإعدادات النهائية، التوقيع، وتكامل Apple Permissions.

## 2. قيود التخزين والمزامنة (Storage & Sync)
- **المزامنة السحابية:** يتم حفظ بيانات المشاريع محلياً باستخدام `Room Database` لضمان عمل التطبيق بوضع Offline. لم يتم تفعيل المزامنة السحابية الفورية (Cross-device Sync) في هذا الإصدار لتقليل تكلفة Firestore.

## 3. الذكاء الاصطناعي وحدود الاستخدام
- **توليد النصوص:** يعتمد على نموذج Gemini API (عبر Backend). قد يستغرق التوليد بعض الوقت (High Latency) في أوقات الذروة.
- **حدود التوليد:** الاستخدام مقيد بحد شهري (Token Limit) لحماية المنصة من الاستهلاك المفرط (تديره Cloud Functions).
- **القيود الفقهية:** النموذج غير مصرح له بالإفتاء. أي ادعاء شرعي يولده النموذج يجب أن يوضع تحت المراجعة (Pending Review) قبل اعتماد النشر.

## 4. قيود معالجة وتصدير الفيديو
- **الأداء:** معالجة الفيديوهات محلياً (On-device Rendering) قد تؤدي إلى بطء في الأجهزة ذات الإمكانيات الضعيفة، أو قد تتأثر عند قفل الشاشة (بانتظار تحسين WorkManager مستقبلاً).
- **دعم الخطوط العربية المعقدة:** بعض أنماط الخط العثماني قد تظهر بشكل غير مثالي أثناء تصدير الفيديو ويتم عرضها بالخط العربي القياسي بدلاً من ذلك.
