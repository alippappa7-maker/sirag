## [PROMPT 034] - 2026-08-27 (بناء قسم المحراب)


### أُضيف (Added)
- **واجهة المحراب (`MihrabScreen`) و (`MihrabViewModel`):**
  - تم تصميم واجهة قسم المحراب لتشمل (القرآن، التلاوات، التفاسير، الأذكار، مواقيت الصلاة، القبلة، التقويم الهجري، والآيات المحفوظة).
  - دعم بحث موحد داخل القسم في السور والتفاسير والأذكار.
  - قسم لمتابعة القراءة (آخر آية) ومتابعة الاستماع (آخر تلاوة).
  - قسم للوصول السريع للأدوات اليومية كالأذكار والقبلة.
  - تصميم هادئ وواضح مع دعم `RTL` واعتماد تصميم `Material 3` والـ `Grid` المبسط.
  - تم الالتزام بعدم وضع نصوص قرآنية وهمية، واستخدام نماذج بنيوية بدلاً من ذلك.
  - التعامل مع حالات عدم الاتصال بالشبكة (Offline) وأخطاء التحميل.
- **تحديث التنقل (Navigation):**
  - تغيير اسم علامة التبويب في شريط التنقل السفلي إلى "المحراب" ليناسب شمولية القسم.

## [PROMPT 033] - 2026-08-27 (مشغل الفيديو - Video Player)

### أُضيف (Added)
- **مشغل فيديو قابل لإعادة الاستخدام (`SirajVideoPlayer`):**
  - دعم تشغيل وإيقاف مؤقت مع معاينة صورة مصغرة (Thumbnail) قبل التشغيل.
  - واجهة تحكم مخصصة (Custom UI) فوق المشغل تدعم تغيير سرعة العرض (0.5x, 1.0x, 1.5x, 2.0x)، كتم الصوت وإلغاء الكتم.
  - شريط تقدم مخصص (Slider) يدعم التقديم والتأخير (Seek) واستعادة الموضع.
  - أزرار للمشاركة والتنزيل مع دعم دوال خارجية للاستدعاء.
  - دعم زر وتغيير وضع ملء الشاشة.
  - دعم أبعاد ونسب الفيديو المختلفة (RESIZE_MODE_FIT و RESIZE_MODE_ZOOM) لتعمل مع الفيديوهات الطولية والمربعة والعريضة.
  - دعم الحروف العربية وتهيئة عرض الوقت بصيغة (LTR) لمنع انقلاب التوقيت.
  - تفويض تشغيل الفيديو لتطبيق إعدادات المستخدم المتعلقة بالتشغيل التلقائي (`autoPlay`).
- **إدارة الحالات والاستقرار:**
  - إيقاف المشغل تلقائيًا (Pause) عند خروج التطبيق للخلفية (Lifecycle Handling) باستخدام `LifecycleEventObserver`.
  - معالجة وإظهار حالة التخزين المؤقت (Buffering) للمشاهد.
  - التخلص التام من الموارد (Release) عند التدمير لعدم تحميل الذاكرة فيديوهات كاملة.
  - عرض رسائل خطأ دقيقة (Error States) لانتهاء الروابط الموقعة `Signed URLs` (HTTP 403 / 401) أو مشاكل الاتصال بالشبكة مع خيار إعادة المحاولة (Retry).
- **التحديثات الهيكلية:**
  - دمج `androidx.media3` (`media3-exoplayer` و `media3-ui`) لإدارة تشغيل الوسائط بكفاءة.

## [PROMPT 032] - 2026-08-27 (واجهة تصدير الفيديو والتنزيل والمشاركة - Export Video & Sharing)

### أُضيف (Added)
- **واجهة تصدير الفيديو الشاملة (`ProjectExportScreen`):**
  - اختيار مقاس الفيديو (9:16 للريلز، 1:1 انستغرام، 16:9 يوتيوب).
  - اختيار جودة الإخراج (720p SD, 1080p FHD, 4K UHD).
  - اختيار معدل الإطارات (24 fps سينمائي، 30 fps قياسي، 60 fps حركة سلسة).
  - خيارات التضمين: حرق الترجمة المزامنة، التوثيق الشرعي واسم المصدر، والعلامة المائية لمنصة سراج.
  - التبديل بين وضع المعاينة السريعة (10 ثوانٍ) والتصدير النهائي الكامل.
- **عرض التكلفة الشفاف والحدود (Costs & Limits):**
  - حساب التكلفة الحركية بالنقاط استناداً للمدة، الجودة، معدل الإطارات ووضع المعاينة.
  - عرض رصيد النقاط المتاح في الحساب والتحقق من كفايته قبل التصدير.
  - مؤشر استهلاك مساحة التخزين في المساحة السحابية (MB).
- **فحص الموانع والتحذيرات ما قبل التصدير (Pre-Export Validation):**
  - فحص وجود مشاهد في المشروع، وجود نصوص مسموعة/معروضة، ووجود ترجمة نصية.
  - حظر التصدير في حال وجود موانع حرجة (`BLOCKER`).
  - إظهار نافذة تأكيد للمستخدم لتنبيهات قبل التصدير القابلة للتجاهل (`WARNING`).
- **متابعة حية وإجراءات الملف الناتج (Active Job Tracker & Export Actions):**
  - شريط تقدم حي بنسبة المئوية وسجل تنفيذ المعالجة خطوة بخطوة.
  - إمكانية إلغاء عملية التصدير واسترداد الرصيد.
  - إمكانية تنزيل الفيديو الناتج عبر فتح الرابط الموقع.
  - إمكانية مشاركة رابط الفيديو أو نسخه مع تنبيه باعتام الصلاحية المؤقتة (7 أيام).
  - إمكانية حذف الملف النهائي الناتج من التخزين السحابي لإخلاء المساحة دون التأثير على المشروع الأصلي.
- **اختبارات الوحدة (Unit Tests):**
  - إضافة `ProjectExportViewModelTest.kt` لاختبار حساب التكلفة الديناميكي، فحص الموانع والتحذيرات، وصلاحية الروابط المؤقتة.

## [PROMPT 031] - 2026-08-27 (محرك تركيب وتصيير الفيديو السحابي - Video Composition Service & Cloud Run Engine)

### أُضيف (Added)
- **مخطط تركيب الفيديو (Video Composition Manifest):**
  - نموذج `VideoCompositionManifest` يحتوي على الترتيب الصارم للمشاهد حسب `orderIndex`، أبعاد الفيديو (9:16, 1:1, 16:9)، وخيارات الدقة ومعدل الإطارات والترميز (H.264/AAC).
  - نماذج الطبقات الصوتية `CompositionAudioTrack`، شارات وهوية التوثيق `CompositionBranding`، والترجمة المدمجة `CompositionSubtitleItem`.
- **خدمة التركيب السحابي (Video Composition Service):**
  - بناء وتطبيق `VideoCompositionService` و `FirebaseVideoCompositionServiceImpl` لقراءة المشروع المعتمد، وتطبيق الانتقالات البصرية (Fade, Dissolve, Slide) والتحريك، ودمج الصوتيات.
  - تطبيق الضوابط الشرعية: منع وضع شارة "موثق" إلا إذا كانت حالة مراجعة المشروع `APPROVED`.
  - دعم تصدير نسخ المعاينة السريعة (Preview Clip) ونسخ الماستر عالية الجودة.
  - حفظ الناتج في Google Cloud Storage وتوليد روابط تحميل موقعة مؤقتة (Signed URLs) صالحة لمدة 7 أيام.
  - استرداد الرصيد المحجوز آلياً عند فشل المعالجة.
- **خدمة المعالجة الخلفية (Cloud Run Composition Worker):**
  - إنشاء `server/composition-worker/index.js` المبنية على Cloud Run و FFmpeg للتعامل مع معالجة الفيديو الثقيلة خارج تطبيق العميل.
- **الاختبارات الآلية (Unit Tests):**
  - إضافة `VideoCompositionTest.kt` للتحقق من ترتيب المشاهد، حسابات التكلفة، ضبط الأبعاد، وضوابط التوثيق الشرعي.

## [PROMPT 030] - 2026-08-27 (طابور إنتاج الفيديو - Production Jobs & Cloud Tasks Queue)


### أُضيف (Added)
- **نماذج طابور المهام (Production Job Models):** إضافة `ProductionJob`، `ProductionJobStatus` (QUEUED, PROCESSING, RENDERING, UPLOADING, COMPLETED, FAILED, CANCELLED)، و `ProductionQuality` (SD_720P, FHD_1080P, UHD_4K).
- **مستودع إدارة ومعالجة المهام السحابية (ProductionJobRepository):**
  - إنشاء وإدراج المهام السحابية في طابور Cloud Tasks مع حساب التكلفة خادمياً دون الاعتماد على مدخلات العميل.
  - تطبيق مفتاح عدم التكرار (Idempotency Key) لمنع الازدواجية في الإرسال والخصم المالي.
  - دعم إلغاء المهام قيد المعالجة واسترداد الرصيد المحجوز فورياً (`creditRefunded = true`).
  - دعم إعادة المحاولة المحدودة (Limited Retries بحد أقصى 3 محاولات) مع تسجيل المحاولات في السجلات.
  - متابعة مستمرة وتدفقات حية (`Flow`) لحالة ونسبة تقدم المعالجة.
- **واجهة تتبع وإدارة المهام (ProductionJobsScreen & ProductionJobsViewModel):**
  - حوار بدء تصدير يتيح تخصيص الجودة والأبعاد وخيار حرق الترجمة داخل الفيديو.
  - عرض نسبة الإنجاز اللحظية عبر شريط تقدم (Progress Bar) وحالات المعالجة المختلفة.
  - سجل آمن لأحداث المهمة (Job Logs) دون كشف أي أسرار أو مفاتيح خادمية.
  - توفير روابط تنزيل ومشاركة الفيديو المؤقتة والآمنة فور اكتمال التوليد.
  - ربط شاشة المهام مع شاشة معاينة المشروع `ProjectPreviewScreen` وشجرة الملاحة `AppNavigation`.

## [PROMPT 029] - 2026-08-27 (معاينة المشروع وفحص ما قبل التصدير - Project Preview & Pre-Export Validation)

### أُضيف (Added)
- **نماذج فحص الجودة والتصدير (Pre-Export Domain Models):** إضافة `PreExportValidationIssue`، `ValidationSeverity`، `ValidationIssueType`، و `PreExportReport`.
- **محرك فحص الجودة الشرعية والفنية (Pre-Export Quality Gate):**
  - كشف الموانع الحرجة (Blockers): مشاهد بدون مدة زمنية، تداخل زمني في أسطر الترجمة، أو مشروع فارغ.
  - كشف التحذيرات (Warnings): مشاهد بدون وسائط بصرية، نصوص شرعية غير مربوطة بمصادر موثقة في المحراب، أو أسطر نصوص ممتدة تتجاوز حدود العرض.
  - إيقاف ومنع التصدير النهائي برمجياً طالما هناك موانع حرجة (`isExportAllowed = false`).
  - توفير روابط "إصلاح الآن" داخل التقرير للانتقال الفوري إلى المشهد أو محرر الترجمة المسبب للخطأ.
- **واجهة مشغل المعاينة المتكاملة (ProjectPreviewScreen):**
  - دعم مقاسات العرض المختلفة: 9:16 (Reels/TikTok)، 16:9 (Landscape)، و 1:1 (Square).
  - مسطرة توقيت زمنية متزامنة مع أجزاء الثانية ومشاهد المشروع.
  - أزرار تشغيل/إيقاف مؤقت، كتم الصوت، التحكم في سرعة العرض (0.5x, 1.0x, 1.5x, 2.0x)، والتنقل بين المشاهد.
  - شريط مصغرات تفاعلي للانتقال الفوري بين المشاهد.
  - عرض طبقة الترجمة والشارات متزامنة تماماً مع موقع رأس القراءة (Playhead).

## [PROMPT 028] - 2026-08-27 (الترجمة النصية للفيديو - Subtitles & Timing)

### أُضيف (Added)
- **نماذج الترجمة والشارات (Subtitle Domain Models):** إضافة `SubtitleItem`، `SubtitleStyle`، `SubtitleSourceType`، `SubtitlePosition`، `SubtitleFontFamily`، و `SubtitleReviewStatus`.
- **مستودع الترجمة والتصدير (SubtitleRepository):** تطبيق `FirebaseSubtitleRepositoryImpl` لتخزين واسترجاع أسطر الترجمة، توليد التقسيم الزمني التلقائي من نص المشهد، ترجمة المعاني إلى الإنجليزية، ومحرك تصدير ملفات `SRT` و `VTT`.
- **الضوابط الشرعية والقانونية (Islamic Subtitle Policy & Verification):**
  - قفل النصوص الشرعية (الآيات والأحاديث) برمجياً لمنع إعادة الصياغة أو التحريف، مع السماح بضبط التوقيت الزمني للظهور فقط.
  - تمييز ترجمة المعاني الإنجليزية ووضعها في حالة مراجعة إجبارية `PENDING_REVIEW` مع التنبيه بضرورة اعتماد المترجم وعدم اعتبارها نصاً قرآنياً أصلياً.
  - التحقق من اتساق التوقيت ومنع تجاوز مدة المشهد المحددة.
- **واجهة محرر الترجمة التفاعلية (SubtitleEditorScreen):**
  - نافذة معاينة فيديو تفاعلية مع مسطرة تمرير (Scrubber) لمتابعة تزامن الترجمة مع المشهد لحظياً.
  - أدوات تحرير الأسطر وإضافة أو حذف السطور، وتعديل بدايات ونهايات التوقيت بالثواني.
  - شيت تخصيص المظهر: تغيير موضع الترجمة (أسفل، وسط، أعلى)، الخطوط العربية (Sans, Amiri, Kufic, Noto)، حجم الخط، وخيار حرق الترجمة في الفيديو (Hardsub vs Softsub).
  - تبديل وعرض اللغات وتصدير فوري لصيغتي SRT و VTT مع زر النسخ للحافظة.

## [PROMPT 027] - 2026-08-27 (مكتبة الموسيقى والمؤثرات الصوتية - Soundtracks & SFX Library)

### أُضيف (Added)
- **نماذج البيانات (Soundtrack Domain Models):** إضافة `SoundtrackItem`، `SoundtrackCategory`، `SoundLicenseType`، و `SceneAudioTrackConfig`.
- **مستودع المؤثرات والموسيقى (SoundtrackRepository):** تطبيق `FirebaseSoundtrackRepositoryImpl` لتوفير مكتبة غنية من أصوات الطبيعة، مؤثرات SFX، فوكالز بشرية، وألحان مرخصة مع توثيق الترخيص والنسب وتحديث طبقات الصوت للمشهد في `Firestore`.
- **سياسة الصوت وضوابط المحتوى (Audio Policy & Islamic Guardrails):**
  - فلتر إجباري لعزل وإخفاء الموسيقى، والاكتفاء بالأصوات الطبيعية والفوكالز والمؤثرات.
  - عدم فرض الموسيقى كعنصر إلزامي وإمكانية تصدير المشهد بدون أي طبقة صوتية اختيارية.
  - منع استخدام التلاوة القرآنية كخلفية تلقائية.
  - الشفافية في عرض التراخيص وتحديد ما إذا كان يتطلب نسبة المؤلف في شارة النهاية.
- **واجهة مكتبة المؤثرات (SoundtrackLibraryScreen):**
  - شريط بحث وفلاتر تصنيف حية.
  - مشغل معاينة فورية للصوت.
  - نافذة ضبط طبقة الصوت للمشهد: التحكم بمستوى الصوت (Volume Slider)، التكرار المستمر (Looping)، وتلاشي الدخول والخروج (Fade-In / Fade-Out).
  - بطاقة استعراض الصوت النشط في المشهد مع زر الحذف والإزالة.

## [PROMPT 026] - 2026-08-27 (الصوت الاصطناعي والتسجيل - Audio Studio & Voiceover)

### أُضيف (Added)
- **نماذج البيانات (Audio Domain Models):** إضافة `AudioItem`، `AudioSourceType` (مقسم إلى `GENERATED_VOICE`, `USER_RECORDING`, `LICENSED_AUDIO`, `QURAN_RECITATION`)، `VoiceOption`، `AudioLanguage`، و `GenerateAudioRequest`.
- **مستودع الصوتيات والتسجيل (AudioRepository):** تطبيق `FirebaseAudioRepositoryImpl` لدعم توليد الصوتيات، رفع التسجيلات، قص الصوت (Trimming)، والربط المباشر بالمشهد وتحديث التخزين السحابي `Firebase Storage` و `Firestore`.
- **الضوابط الشرعية والقانونية (Islamic Guardrails & Legal Attribution):**
  - منع استخدام الصوت المولد كتلاوة قرآنية.
  - منع انتحال أو تقليد أصوات حقيقية للعلماء والقراء دون إذن موثق.
  - وسم إجباري دائم للشارات (`AI-VOICE` مقابل `تلاوة` و `تسجيل`) مع نص إخلاء المسؤولية الشرعي.
- **واجهة الاستوديو الصوتي الشاملة (AudioStudioScreen):**
  - **تبويب التوليد الصوتي:** دعم معلقين صوتيين باللغة العربية الفصحى واللهجات الوثائقية مع تخصيص السرعة ونبرة الوقار (Pitch) واحتساب التكلفة وخصم الرصيد.
  - **تبويب رفع التسجيلات والتلاوات:** رفع ملفات MP3/WAV/M4A/AAC حتى 50MB مع توثيق اسم القارئ أو المتحدث ونوع الترخيص.
  - **تبويب أصوات المشروع:** مشغل صوتيات مدمج للمعاينة الفورية، حذف، أداة قص تفاعلية للبداية والنهاية، وزر ربط مباشر بالمشهد ومزامنة زمن العرض.
- **دالة سحابية (Cloud Functions):** نقطة النهاية `generateVoiceover` للتحقق الآمن والخصم التلقائي للرصيد مع الاسترجاع عند الفشل.

## [PROMPT 025] - 2026-08-27 (توليد الصور بالذكاء الاصطناعي - Imagen 3 والضوابط الشرعية)

### أُضيف (Added)
- **النماذج والكيانات (AI Image Generation Models):** إنشاء `AiImageGenerationRequest`، `GeneratedImageItem`، `AiImageStyle`، `AiImageAspectRatio`، و `AiImageStatus`.
- **مستودع توليد الصور (AiImageRepository):** إنشاء واجهة `AiImageRepository` مع تطبيق `FirebaseAiImageRepositoryImpl` للاتصال بـ Cloud Functions و Firestore مع دعم استرجاع الرصيد عند الإلغاء أو الفشل.
- **الفحص الشرعي الصارم (Islamic Safety Guardrails):** فحص الكلمات المحظورة في الواجهة والخادم (الأنبياء، الرسل، الصحابة، الملائكة، الذات الإلهية).
- **واجهة التوليد البصري (AiImageGeneratorScreen):**
  - اختيار الأنماط الفنية المتوافقة والحديثة (فن إسلامي، فن مصغر، واقعي، سينمائي، إلخ).
  - تحديد الأبعاد (16:9، 9:16، 1:1، 4:5) مع حساب التكلفة وعرض الرصيد المتبقي.
  - إمكانية الإلغاء أثناء التوليد واسترداد الرصيد تلقائياً.
  - بطاقات عرض الصور المولدة مع شارة "AI-GENERATED" وإخلاء المسؤولية الشرعي.
  - تعيين الصورة المولدة مباشرة كخلفية للمشهد المفتوح أو حفظها في مكتبة وسائط المشروع.
- **Cloud Functions:** دوال الخادم `generateImage`، `cancelImageGeneration`، و `deleteGeneratedImage` مع التحقق الخادمي من الرصيد والضوابط الشرعية.

## [PROMPT 024] - 2026-08-27 (البحث في الوسائط الخارجية وإدارة التراخيص)

### أُضيف (Added)
- **نماذج البحث والتراخيص (External Media Models):** `ExternalMediaItem`, `MediaProvider`, `LicenseType`, `ExternalMediaSearchFilter`.
- **مستودع الوسائط الخارجية (ExternalMediaRepository):** جلب ومعاينة الوسائط من مصادر مفتوحة ومرخصة (Unsplash, Pexels, Pixabay, Wikimedia Commons, Internet Archive).
- **التحقق من التراخيص (License Verification):** التحقق من شروط الاستخدام التجاري والتعديل ومتطلبات النسب.
- **واجهة البحث المتقدم (ExternalMediaSearchScreen):** تنقية بالنوع والترخيص والأبعاد ومصدر الوسائط، مع زر استيراد مباشر وحفظ الترخيص في مكتبة أصول المشروع.

## [PROMPT 023] - 2026-08-27 (مدير الوسائط)

### أُضيف (Added)

- **نموذج البيانات Asset:** تم بناء النموذج لمعالجة الوسائط (image, video, audio, music, font, subtitle) مع بيانات الترخيص والمصدر.

- **الطبقة البرمجية (Repository):** إنشاء `AssetRepository` و `FirebaseAssetRepositoryImpl` لدمج Firestore و Firebase Storage.

- **واجهة مدير الوسائط (AssetLibraryScreen):** شاشة تتيح عرض الأصول كشبكة، ورفع أصل جديد مع إدخال بياناته الوصفية.

- **تحذيرات الأصول المستخدمة:** تم دمج فحص في `AssetLibraryViewModel` يمنع/يحذر من حذف أصل قيد الاستخدام في أي مشهد من مشاهد المشروع.

- **تحديث Navigation:** تم إضافة زر في `ProjectEditorScreen` للوصول السريع إلى مدير الوسائط.




# سجل تغييرات المشروع (Changelog)

## [PROMPT 022] - 2026-08-27 (محرر المشهد)
### أُضيف (Added)
- شاشة `SceneEditorScreen` مخصصة لتعديل المشهد بشكل مفصل وداعم للحفظ التلقائي.
- دعم تعديل النص الظاهر على الشاشة `SceneText` مع تنسيقاته (الحجم، اللون، المحاذاة، الموضع).
- ميزة التراجع والإعادة (Undo/Redo) للحفظ التلقائي داخل المحرر.
- ميزة استنساخ المشهد (Duplicate) مباشرة من داخل المحرر.
- إضافة معاينة حية سريعة `ScenePreviewBox` للنصوص والخلفية داخل المحرر.
- رسالة تحذير وإعادة حالة المراجعة للمشروع (`CHANGES_REQUESTED`) بشكل تلقائي عند تعديل نص مشهد مرتبط بادعاء شرعي موثق (حماية المحتوى المراجع).

## [PROMPT 021] - 2026-08-27 (إنتاج المشاهد)
### أُضيف (Added)
- نماذج بيانات المشاهد: `Scene`, `SceneAsset`, `SceneText`, `SceneAudio` في `Project.kt`.
- إمكانية الإضافة، الحذف، النسخ، وإعادة الترتيب للمشاهد من شاشة `ScenesScreen`.
- حساب المدة الكلية للمشروع بناءً على مدة المشاهد المجمعة وتحديثها تلقائياً.
- ميزة التراجع (Undo) في حالة حذف أو تعديل أو ترتيب المشاهد.
- منع حذف المشاهد المعتمدة، وفرض إعادة مراجعة المشروع قبل تعديلها.
- إمكانية ربط المشهد بالادعاءات (Claims) الموجودة في السيناريو لاحقاً.
- واجهة اختيار مبدئية لربط الوسائط بالمشهد.

## [PROMPT 020] - 2026-08-27 (إرسال المحتوى للمراجعة)
### أُضيف (Added)
- حالات المراجعة `ReviewState` وسجل المراجعة `ReviewLog` في `Project.kt`.
- شاشة إرسال للمراجعة مع فحص تلقائي للادعاءات غير الموثقة ودرجة الخطورة في `ContentPlanScreen.kt`.
- أداة واجهة للمراجع (Reviewer) لقبول أو رفض أو طلب تعديل مع تسجيل التعليقات والوقت.
- قفل إمكانية التعديل للـ Creator أثناء المراجعة وبعد الاعتماد.

## [PROMPT 012-019] - 2026-08-27 (ميزات سابقة في استوديو المحتوى)
### أُضيف (Added)
- مساحات العمل والتعاون.
- واجهة استوديو المحتوى.
- قوالب المحتوى العربي ومولد الأفكار.
- تكامل Gemini الآمن.
- خطة المحتوى والسيناريو.
- طبقة المصادر والتحقق الأولي.
- تكامل القرآن والتفسير باستخدام القرآن كمرجع موثوق.



## [PROMPT 011] - 2026-08-27 (نموذج بيانات المشاريع)
### أُضيف (Added)
- **نماذج البيانات الكاملة:** إنشاء `Project`, `ProjectMember`, `ProjectVersion`, `ProjectAsset`, `ProjectActivity` لتشمل جميع الحقول المطلوبة لبناء محرر الوسائط مستقبلاً.
- **حالات المشروع الجديدة:** تحديث الحالات لتتوافق مع نظام المعالجة (`DRAFT`, `PROCESSING`, `READY`, `EXPORTING`, `COMPLETED`, `FAILED`, `ARCHIVED`, `DELETED`).
- **تحديث مستودع المشاريع (`ProjectRepository`):**
  - دعم البحث (Search) والفرز (Sort) وتقسيم الصفحات (Pagination عبر `limit` حالياً).
  - إضافة واجهة لإنشاء وقراءة النسخ (Versioning).
  - تسجيل نشاطات المشروع (`ProjectActivity`) بشكل تلقائي عند الإنشاء أو التعديل (Auto-save) أو إنشاء نسخ.
- **تحديث قواعد `firestore.rules`:** حماية مجموعات `project_activities` و `project_versions`.

## [PROMPT 010] - 2026-08-27 (قسم الإعدادات والتفضيلات)
### أُضيف (Added)
- **شاشة الإعدادات الشاملة (SettingsScreen):** تصميم جديد كلياً يعتمد على قائمة رئيسية مع قوائم فرعية للتفضيلات.
- **إدارة التفضيلات (UserPreferences):** توسيع النموذج ليشمل السمة (المظهر)، تقليل الحركة، لغة العرض، المدينة (لحساب المواقيت)، إعدادات الإشعارات، إعدادات المحراب، الجودة، وقفل التطبيق.
- **مزامنة التفضيلات (Sync):** تم تحديث `AuthRepository` و `FirebaseAuthRepositoryImpl` لحفظ الإعدادات بشكل متزامن في Firestore وتطبيقها محلياً فوراً.
- **حذف الحساب:** إضافة خيار لحذف الحساب نهائياً مع نافذة تأكيد تحذيرية لحماية المستخدم.
- **السمة التلقائية:** تم ربط `MainActivity` مع التفضيلات لتحديث سمة التطبيق (فاتح/داكن/تلقائي) بمجرد تغييرها.

## [PROMPT 009] - 2026-08-27 (الرئيسية وإدارة المشاريع)
### أُضيف (Added)
- **نموذج المشاريع (Project Model):** تم إنشاء `Project` مع دعم `id, ownerId, status, scenes, assets, version, timestamps`.
- **مستودع المشاريع (ProjectRepository):** تم تنفيذ `FirebaseProjectRepositoryImpl` مع دعم إنشاء، نسخ، حذف (Soft Delete)، أرشفة واستعادة.
- **الصفحة الرئيسية (HomeScreen):** تصميم الواجهة الجديدة لعرض الترحيب، زر إنشاء فيديو، اختصارات المحراب والومضات، وقائمة "المشاريع الأخيرة".
- **إدارة المشاريع (StudioScreen):** صفحة لإدارة جميع المشاريع، تدعم البحث (Search)، التصفية (Filter)، الترتيب (Sort)، بالإضافة لخيارات النسخ والأرشفة والحذف.
- **محرر المشروع (ProjectEditorScreen):** واجهة لفتح المشروع مع دعم **الحفظ التلقائي** (Auto-Save مع `debounce 1500ms`) عبر `ProjectEditorViewModel`.
- **قواعد Firestore (Firestore Rules):** تم تحديث قواعد Firestore للمشاريع بحيث لا يرى المستخدم إلا مشاريعه أو ما تمت مشاركته معه، ولا يستطيع الحذف إلا مالك المشروع.


## [PROMPT 008] - 2026-08-27 (الحساب والأدوار والصلاحيات)
### أُضيف (Added)
- **ملف المستخدم (UserProfile):** تم تحديث نماذج البيانات وتحديث `AuthRepository` ليعيد `UserProfile` الكامل للمستخدم بما يشمل بيانات دوره (Role).
- **إدارة الأدوار (Role-Based Access Control):** إنشاء نظام أدوار: `USER`, `CREATOR`, `REVIEWER`, `ADMIN`, `OWNER`.
- **شاشة الملف الشخصي (ProfileScreen):** تم إنشاء شاشة الملف الشخصي كبديل لشاشة الإعدادات، تتيح للمستخدم تعديل بياناته (الاسم والصورة) وعرض دوره الحالي وتسجيل الخروج.
- **حراسة المسارات (Route Guards):** تعديل `AppNavigation` لحماية `AdminScreen` والسماح فقط للأدوار الإدارية (`ADMIN`, `OWNER`) بالوصول، وعرض شاشة خطأ مخصصة للمستخدمين غير المصرح لهم.
- **قواعد Firestore (Firestore Rules):** تحديث `firestore.rules` لإضافة القواعد التي تمنع المستخدم العادي من تعديل دوره (Role)، رصيده، أو خطته. وتسمح بالوصول لبيانات كل مستخدم حسب دوره، مع منع المدراء من ترقية حساب إلى مالك (OWNER) إلا إذا كان المنفذ مالكاً.


## [PROMPT 007] - 2026-08-27 (ربط Firebase والمصادقة)
### أُضيف (Added)
- **خدمات Firebase:** إضافة تبعيات `firebase-auth`، `firebase-firestore` و Play Services Credentials، واستخدام إعدادات `google-services` الجاهزة في المشروع.
- **طبقة مستودع المصادقة (AuthRepository):** إنشاء واجهة `AuthRepository` و تطبيقها في `FirebaseAuthRepositoryImpl` للتعامل مع تسجيل الدخول، إنشاء الحساب، التحقق، استعادة الكلمة، وحذف الحساب.
- **إدارة الحالة (AuthViewModel):** إنشاء `AuthViewModel` يستخدم تدفقات الحالة `StateFlow` للتعامل مع واجهات التطبيق وتوفير حالة الجلسة (`Loading`, `Success`, `Error`).
- **حماية المسارات (Route Guards):** تحديث `AppNavigation` للاعتماد على الحالة الفعلية للمستخدم عبر Firebase Auth بدلاً من الحالة الوهمية، وإضافة شاشة `RegisterScreen`.
- **قواعد الحماية الأولية (Firestore Rules):** إنشاء ملف `firestore.rules` لمنع أي مستخدم من قراءة أو كتابة بيانات مستخدم آخر (`users/{userId}`).

## [PROMPT 006] - 2026-08-26 (مكونات الواجهة والنماذج المؤقتة)
### أُضيف (Added)
- **نماذج البيانات التجريبية (Preview Models):** إنشاء نماذج مبسطة (UserPreview, ProjectPreview, VideoPreview, AudioItem, FlashItem, SourcePreview, NotificationPreview, SubscriptionPreview).
- **بيانات تجريبية مفصولة (Mock Data):** إنشاء ملف `MockData.kt` مستقل تماماً لضمان عدم تداخله مع طبقة الإنتاج (Production / Firebase Repositories).
- **مكونات الواجهة (UI Components):** 
  - بناء بطاقات العرض: `SirajProjectCard`، `SirajVideoCard`، `SirajAudioCard`، `SirajSourceCard`، و `SirajFlashCard`.
  - بناء شارة التحقق الشرعي `VerificationStatusBadge`.
  - بناء مؤشر التقدم `SirajProgressBar` وقائمة التحميل الهيكلية `SkeletonList`.
  - بناء نافذة التأكيد `SirajConfirmationDialog`.
- دمج المكونات والبيانات التجريبية في شاشات التطبيق (`HomeScreen`, `StudioScreen`, `AudioLibraryScreen`, `FlashesScreen`, `DetailsScreen`) لاختبار الواجهة قبل ربط الخدمات الخارجية.

## [PROMPT 005] - 2026-08-26 (هيكل التطبيق والتنقل)
### أُضيف (Added)
- **الهيكل الملاحي (App Navigation):** إضافة كافة شاشات المشروع الأساسية (Splash, Onboarding, Login, Main Tabs, Settings, Admin, Details).
- **التنقل المتكيف (Adaptive Navigation):** بناء `MainShellScreen` الذي يعرض شريط تنقل سفلي (`BottomNavigationBar`) للهواتف، وشريط تنقل جانبي (`NavigationRail`) للشاشات الكبيرة (الأجهزة اللوحية).
- **الروابط العميقة (Deep Links):** تجهيز صفحة `DetailsScreen` وتكوينها لدعم الروابط العميقة `siraj://details/{id}`.
- **حارس المصادقة (Auth Guard):** تطبيق حارس تنقل مبدئي يمنع الوصول لصفحات المحتوى والإدارة دون تسجيل دخول، ويعيد التوجيه لصفحة `Login`.
- استخدام شاشات الحالة `EmptyScreen` و `ErrorScreen` لملء الصفحات الفارغة للمكتبة الصوتية والومضات.

## [PROMPT 004] - 2026-08-26 (الهوية ونظام التصميم)
### أُضيف (Added)
- **الألوان (Colors):** توحيد الألوان باستخدام الهوية البصرية (الكحلي العميق للأساسي، الأزرق السماوي للثانوي، الأبيض، ولمسات ذهبية).
- **الخطوط (Typography):** ضبط أوزان وأحجام النصوص (Typography) لدعم القراءة باللغة العربية بناءً على خطوط النظام الافتراضية مع مسافات مريحة.
- **المسافات والانحناءات (Shapes):** ضبط حواف البطاقات والأزرار (8dp، 16dp) لتكون مريحة للعين.
- **المكونات المشتركة:** تحديث `SirajButton`، `SirajCard`، و `SirajTextField` لاستخدام قيم الـ `MaterialTheme` بشكل صارم وتوفير معاينات للوضعين الفاتح والداكن.
- **حركات الانتقال:** إضافة حركات تلاشي هادئة (`Fade`) لتنقلات الصفحات داخل `AppNavigation`.
### قرارات تقنية (Technical Decisions)
- تعطيل `dynamicColor` في الثيم افتراضياً لفرض هوية "سراج" البصرية وعدم الاعتماد على ألوان خلفيات النظام، مما يضمن ظهور التطبيق بشكل متناسق دائماً.

## [PROMPT 003] - 2026-08-26 (تأسيس بنية المشروع)
### أُضيف (Added)
- تأسيس بنية المشروع باستخدام Clean Architecture (Feature-First) بما يتوافق مع بيئة `Kotlin/Compose`.
- إنشاء حزم: `core`, `domain`, `data`, `features`.
- إعداد ملفات البيئة (`.env` و `.env.example`).
- إضافة صفحات واجهات فارغة كمكونات للحالات: `LoadingScreen`, `ErrorScreen`, `EmptyScreen`, `OfflineScreen`.

## [PROMPT 002] - 2026-08-26 (التخصيص ونظام حفظ السياق)
### أُضيف (Added)
- إنشاء ملفات التوثيق وحفظ السياق: `PROJECT_CONTEXT.md`، `README.md`، `ARCHITECTURE.md`، `SECURITY.md`، `TEST_PLAN.md`، `KNOWN_LIMITATIONS.md`.

## [PROMPT 001] - 2026-08-26 (تأسيس المشروع)
### أُضيف (Added)
- إنشاء تطبيق Android أولي باستخدام `Jetpack Compose` و `Kotlin`.
- تحديث `applicationId` و `namespace`.

### [0.24.0] - 2026-08-27
#### Added
- طبقة `MediaSearchProvider` للبحث عن الوسائط الخارجية (صور وفيديو).
- شاشة `ExternalMediaSearchScreen` للبحث والتصفية (النوع، الاتجاه، اللون، المدة) مع إمكانية عرض معاينة.
- إضافة الأصل الذي تم البحث عنه إلى المشروع مباشرة مع تسجيل اسم المصدر والترخيص ونص النسبة.
- تطبيق نظام `Pagination` عبر `nextPageToken`.
- تطبيق الـ Mocking للمزود الخارجي بشكل يسهل استبداله بـ API حقيقي (عبر الخادم) لاحقاً.
