# نظام أمان الحسابات الإدارية (Admin Security Controls)
**منصة سراج (Siraj Platform)**

---

## 1. الهوية والهدف
يضمن هذا النظام حماية الحسابات ذات الصلاحيات العالية (مثل `OWNER`, `ADMIN`, `REVIEWER`) من الاختراق وسوء الاستخدام، حيث أن سرقة جلسة أو استغلال جهاز مفتوح قد يؤدي إلى مساس بالنظام ككل.

---

## 2. مستويات الحماية والقيود
تم تطبيق القواعد الآتية على جميع الحسابات الإدارية:

### أ. المصادقة المتعددة العوامل (MFA)
- لا يمكن لأي حساب إداري بدء جلسة (Session) مالم يجتز التحقق بخطوتين (MFA). 
- عدم توفر MFA يمنع تسجيل الدخول الإداري كلياً.

### ب. إعادة التحقق للعمليات الحساسة (Re-Authentication)
- عند القيام بعمليات حساسة (تصدير بيانات، تغيير صلاحيات، تعديل أسرار)، يُطلب من الإداري إعادة إدخال رمز التحقق (أو بصمة) إذا كان الجهاز غير موثوق بالكامل، لمنع استغلال الأجهزة المتروكة.

### ج. مهلة عدم النشاط (Idle Timeout)
- تُعلق الجلسة تلقائياً إذا مضى وقت محدد (مثلاً 15 دقيقة) دون نشاط إداري.

### د. حماية الأجهزة والثقة (Device Trust & Signals)
- يتم تتبع الجهاز وعنوان الـ IP عند كل جلسة، ويمكن مراجعتها من لوحة الأمان، ويتم إنهاء الجلسات السابقة لنفس الجهاز لتفادي اختطاف الجلسات (Session Hijacking).

---

## 3. العمليات الحساسة المشمولة (Sensitive Operations)
يخضع تنفيذ هذه العمليات لمحرك حوكمة الصلاحيات (AdminSecurityEngine):
1. `CHANGE_PERMISSION` (تغيير صلاحيات وحزم الموظفين)
2. `MODIFY_VERIFIED_SOURCE` (تعديل مصدر موثق أو شرعي)
3. `DELETE_CONTENT` (حذف محتوى مستخدمين)
4. `CHANGE_BALANCE` (تعديل رصيد)
5. `MODIFY_SUBSCRIPTION` (تعديل اشتراك)
6. `CHANGE_COST_LIMITS` (تغيير حدود تكلفة الورش)
7. `ROTATE_SECRET` (تدوير المفاتيح والأسرار)
8. `BULK_PUBLISH` (نشر جماعي)
9. `EXPORT_DATA` (تصدير بيانات حساسة)

---

## 4. نموذج أقل امتيازاً (Principle of Least Privilege)
- **المالك (`OWNER`)**: يملك صلاحيات كاملة، ويستطيع ترقية آخرين إلى `ADMIN` أو `OWNER`.
- **المشرف (`ADMIN`)**: يملك معظم الصلاحيات باستثناء ترقية الآخرين لمستويات عُليا أو تصدير كافة البيانات.
- **المراجع (`REVIEWER`)**: صلاحياته محصورة فقط في تعديل المصادر وحذف المحتوى المخالف، ولا يملك وصولاً لبيانات مالية أو تصدير أو أسرار.

---

## 5. لوحة أمان الإدارة (Admin Security Dashboard)
يستطيع الإداري من خلال لوحته:
- رؤية حالة حسابه وصلاحياته.
- مراقبة كافة جلساته النشطة (نوع الجهاز، الـ IP، وتاريخ الدخول).
- إبطال وإنهاء (Revoke) أي جلسة مشبوهة أو غير مستخدمة بضغطة زر.

---

## 6. القواعد الحتمية (Non-Negotiable Rules)
1. **لا تعتمد على إخفاء اللوحة فقط**: الحماية مطبقة في قلب المحرك (Backend/Engine) ولا يكتفى بإخفاء الأزرار عن المستخدم العادي.
2. **لا يتم تخزين أسرار أو رموز MFA**: لا تحفظ رموز المصادقة كنصوص صريحة.
3. **لا يمكن مشاركة الحساب**: الجلسات المتعددة غير العادية تخضع للمراقبة وتُنهى للحفاظ على تتبع التدقيق.
4. **تدقيق كامل (Audit Log)**: كل عملية حساسة تُسجل في `SecurityAuditLog` ولا يمكن للمشرف حذف هذا السجل.

---

## 7. اعتماد وثيقة أمان الإدارة
- **المحرر:** مهندس الأنظمة الأمنية - منصة سراج.
- **الحالة:** معتمد ونافذ برمجياً (PROMPT 092).
