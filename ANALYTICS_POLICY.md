# سياسة التحليلات والاستخدام (Analytics Policy)

## المبدأ الأساسي (Privacy-First)
يلتزم تطبيق "سراج" بحماية خصوصية المستخدمين بشكل صارم. تم تصميم نظام التحليلات ليكون **privacy-first** بحيث يجمع فقط البيانات الضرورية لتحسين جودة التطبيق وأدائه، مع استبعاد أي بيانات حساسة أو شخصية.

## البيانات التي لا نجمعها أبداً
1. **النصوص والمحتوى**: لا يتم تسجيل أي نصوص يكتبها المستخدم أو أفكار أو محتوى يتم إنشاؤه.
2. **عناوين الـ IP والموقع الدقيق**: لا نقوم بتسجيل عنوان الـ IP في قاعدة بيانات التحليلات، ولا نجمع الموقع الجغرافي الدقيق.
3. **المفاتيح والأسرار**: لا يتم تسجيل أو تتبع أي مفاتيح API (مثل مفاتيح Gemini) أو Authentication Tokens.
4. **المعلومات الدينية أو الشخصية**: لا نستنتج أو نسجل توجهات المستخدم الدينية أو المذهبية بأي شكل من الأشكال في نظام التحليلات.
5. **المصادر الحساسة**: لا يتم تتبع روابط المصادر الدقيقة لتجنب الكشف عن اهتمامات المستخدمين.

## المخطط الموحد للأحداث (Unified Event Schema)
نقوم بتسجيل مجموعة محددة من الأحداث لمعرفة كيفية استخدام التطبيق ومواضع التحسين:
- `app_opened`: عند فتح التطبيق.
- `onboarding_completed`: عند إتمام جولة البداية.
- `project_created`: عند إنشاء مشروع جديد.
- `project_export_started`: عند بدء تصدير مشروع.
- `project_export_completed`: عند نجاح التصدير.
- `template_used`: عند استخدام قالب (يتم إرفاق معرف القالب فقط).
- `source_attached`: عند إضافة مصدر (بدون الرابط، فقط لمعرفة الميزة).
- `review_submitted`: عند إرسال مادة للمراجعة.
- `flash_published`: عند نشر فيديو قصير.
- `audio_started`: عند تشغيل مقطع صوتي.
- `video_started`: عند تشغيل فيديو.
- `subscription_viewed`: عند استعراض صفحة الاشتراكات.
- `purchase_completed`: عند إتمام الشراء.
- `error_occurred`: عند حدوث خطأ تقني غير متوقع.

## موافقة المستخدم (User Consent)
- لا يتم إرسال أي بيانات تحليلات قبل موافقة المستخدم.
- يمكن للمستخدم **تعطيل التحليلات كلياً** من خلال: (الإعدادات > الخصوصية وسجل النشاط > مشاركة بيانات الاستخدام).

## إخفاء الهوية وتجزئة المعرفات
- يتم استخدام دالة تجزئة (SHA-256) لمعرف المستخدم (User ID) قبل إرساله لنظام التحليلات، مما يجعل تتبع المستخدم بشكل فردي مستحيلاً.

## احتفاظ محدود بالبيانات
- يتم الاحتفاظ ببيانات التحليلات لفترة محدودة، ويتم مسح البيانات المرتبطة بالمستخدم تلقائياً أو عند طلبه مسح بياناته من الإعدادات.

## فصل البيانات
- يتم تخزين أحداث التحليلات في مجموعة (`analytics_events`) مفصولة كلياً عن محتوى مشاريع المستخدم وقاعدة بياناته الأساسية.
- لا يتم الاعتماد على بيانات التحليلات لاتخاذ أي قرار شرعي أو إداري للمستخدمين، هي للتحسين التقني فقط.

## تحليلات صناع المحتوى (Creator Analytics)
- يتم تزويد صناع المحتوى بلوحة تحليلات تعرض الأداء الفني للومضات المنشورة.
- تشمل الأرقام (المشاهدات الكلية، المشاهدات الفريدة التقديرية، التفاعل، والمصادر).
- **منع الربط الديني بالانتشار**: يتم التنويه صراحة بأن الأرقام والإحصاءات مخصصة لتحليل "الأداء الفني" ولا تعد معياراً شرعياً أو دليلاً على قيمة أو صحة المحتوى المنشور.
- لحماية المشاهدين، تعرض تحليلات المبدعين بيانات مجمعة فقط دون كشف أي هويات فردية.
