# سياسة الأرصدة وحدود الاستخدام (Credits & Usage Limits Policy)

هذا المستند يوضح السياسة الخادمية (Server-Side Policy) لإدارة أرصدة المستخدمين وحدود استهلاكهم في تطبيق سراج.

## 1. مصدر الحقيقة (Source of Truth)
- جميع حسابات الأرصدة والمعاملات يتم تقييمها والاحتفاظ بها **على الخادم (Cloud Firestore / Cloud Functions)**.
- يُمنع منعًا باتًا قبول قيم `balance` أو `credits` مباشرة من العميل في طلبات التحديث.
- التطبيق يعرض فقط القيم المسترجعة (Read-Only) من الخادم.

## 2. العمليات الذرية وحجز الأرصدة (Atomic Operations & Reservation)
- للعمليات المكلفة (مثل توليد الصور والصوت)، يجب على العميل طلب إجراء العملية من خلال Cloud Function.
- **الحجز (Reservation):** الخادم ينفذ Firestore Transaction (أو Batched Write):
  1. قراءة `CreditBalance` الحالي.
  2. التأكد من كفاية الرصيد للعملية المطلوبة `amount`.
  3. إنشاء `CreditTransaction` بحالة `RESERVED` وتسجيل `balanceBefore` و `balanceAfter`.
  4. خصم الرصيد من `availableCredits`.
- **التأكيد (Confirmation):** عند نجاح عملية الذكاء الاصطناعي بنجاح، يتم تحديث `status` للمعاملة إلى `COMPLETED`.
- **الإرجاع (Refund):** عند فشل العملية في المزود (Provider)، يتم تحديث `status` للمعاملة إلى `REFUNDED` وإرجاع قيمة `amount` إلى `CreditBalance` في حركة ذرية جديدة.

## 3. منع الازدواجية (Idempotency)
- كل عملية يجب أن تمرر `jobId` فريداً.
- الخادم يتحقق من عدم وجود معاملة مسبقة بنفس `jobId` لمنع الخصم المتكرر لنفس العملية إذا أعاد العميل الإرسال (Retry).

## 4. مساحات العمل (Workspaces)
- إذا كانت العملية مرتبطة بمساحة عمل، يتم التحقق من رصيد مساحة العمل بدلًا من رصيد المستخدم الشخصي.

## 5. تكلفة العمليات (Operation Costs)
- تكلفة العمليات (`amount`) تُدار عبر إعدادات الخادم (Remote Config / Firestore Settings).
- لا تُخزن أرقام التكلفة السرية للمزود (Provider Cost) أو مفاتيحه ضمن تطبيق العميل للحماية من التلاعب العكسي (Reverse Engineering).

## 6. الحدود (Limits)
- يتم تطبيق حدود يومية/شهرية للاستهلاك حسب خطة `Plan` المحددة.
- تُحفظ الحدود في `Entitlement` ويتم التحقق منها خادمياً كطبقة أمان إضافية قبل حجز الرصيد.
