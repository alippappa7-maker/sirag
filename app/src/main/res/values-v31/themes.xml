<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Official AndroidX SplashScreen API Theme for Android 12+ (API 31+) -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_siraj_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="windowSplashScreenAnimationDuration">800</item>
        <item name="postSplashScreenTheme">@style/Theme.MyApplication</item>
        <!-- On Android 12+, display consistent branding footer at the bottom -->
        <item name="android:windowSplashScreenBrandingImage">@drawable/ic_siraj_branding_footer</item>
    </style>
</resources>
