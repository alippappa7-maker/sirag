# خطة التراجع (Rollback Plan)

في حال حدوث عطل حرج (Critical/Blocker) خلال مراحل الإطلاق التدريجي أو الإطلاق الكامل، يتم اتباع الآلية التالية للتراجع الآمن لضمان عدم تأثر المزيد من المستخدمين وتقليل الخسائر.

## 1. التراجع على مستوى متجر التطبيقات (Store Rollback)
- **الإجراء:** إيقاف الإطلاق التدريجي (Halt Rollout).
- **الطريقة:** الدخول إلى لوحة تحكم Google Play Console (أو App Store Connect لاحقاً)، والضغط على "Halt Rollout". هذا يمنع أي مستخدم جديد من تحميل النسخة المعيبة.
- **متى؟** إذا كان العطل يضرب أساسيات التطبيق (Crashes عند التشغيل) ولا يمكن حله من الخادم.

## 2. التراجع عبر رايات الميزات (Feature Flags Kill-switches)
- **الإجراء:** إيقاف الميزة المعيبة دون إيقاف التطبيق بالكامل.
- **الطريقة:** استخدام `Firebase Remote Config` لتعطيل الميزة.
  - مثال: إذا كان الذكاء الاصطناعي يستهلك تكلفة هائلة، يتم تغيير `feature_ai_generation_enabled` إلى `false`.
  - مثال: إذا فشل تصدير الفيديو وتسبب في ANR دائم، يتم إيقاف `feature_video_export_enabled`.
- **متى؟** للأعطال المعزولة داخل ميزات محددة.

## 3. التراجع عن قواعد الأمان أو الوظائف (Backend Rollback)
- **الإجراء:** استعادة النسخة السابقة من Cloud Functions أو Firestore Rules.
- **الطريقة:**
  - `firebase deploy --only functions` بنسخة Git السابقة المؤكدة (Last Known Good Commit).
  - إعادة تطبيق Rules السابقة.
- **متى؟** في حال اكتشاف ثغرة أمنية أو خطأ منطقي يمنع المستخدمين من العمل أو يسمح بتجاوز الاشتراكات.

## 4. التواصل والإصلاح (Hotfix Protocol)
- إبلاغ المستخدمين المتأثرين المتواجدين في النسخة المعيبة عبر `In-App Messaging`.
- تجهيز نسخة ترقيع (Hotfix Build) تتضمن الإصلاح فقط بدون أي ميزات أخرى.
- استئناف الإطلاق (Resume Rollout) باستخدام النسخة المرقعة.
