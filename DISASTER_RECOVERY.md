# خطة التعافي من الكوارث (Disaster Recovery Plan)
**منصة سراج (Siraj Platform)**

---

## 1. أهداف التعافي (Recovery Objectives)
- **RTO (Recovery Time Objective):**
  - **الخدمات الحرجة (قراءة القرآن، الاستعراض):** 1 ساعة كحد أقصى (عبر تفعيل وضع القراءة فقط).
  - **الخدمات الأساسية (الفوترة، المصادقة):** 4 ساعات.
  - **الخدمات المعقدة (إنتاج الفيديو، الذكاء الاصطناعي):** 12 ساعة كحد أقصى للتعافي أو توفير بديل جزئي.
- **RPO (Recovery Point Objective):**
  - **البيانات المالية والمصادقات:** 0 دقيقة (Sync replication، لا مجال لفقدان أي حركات دفع).
  - **مشاريع المستخدمين والمسودات:** أقصاها 12 ساعة (بناءً على وتيرة النسخ الاحتياطي في Storage & Firestore).

---

## 2. آليات النسخ الاحتياطي (Backup Mechanisms)
- **قواعد البيانات (Firestore):** نسخ احتياطي آلي يومي محتفظ به لمدة 30 يوماً.
- **الملفات (Cloud Storage):** تفعيل خاصية الـ Versioning على دلاء (Buckets) التخزين لمنع الفقدان الناجم عن الحذف العرضي أو الخبيث، ونسخ متزامن بين منطقتين (Multi-region).
- **الشفرة والأسرار:** مستودع محمي، وتدوير للمفاتيح في Secret Manager (كما ورد في `SECRETS_LIFECYCLE.md`).

---

## 3. خطط الاستجابة للأعطال التقنية (Technical Outage Playbooks)

### أ. توقف Firebase (Authentication, Firestore, Storage)
- **الأثر:** توقف كامل لمعظم وظائف التطبيق.
- **الخطة:** 
  - إطلاق وضع الصيانة (`FEATURE_MAINTENANCE_MODE = true`).
  - توجيه إشعار للمستخدمين بوجود عطل عالمي في البنية التحتية.
  - منع تسجيل الخروج القسري للحفاظ على حالة الجلسات الحالية قدر الإمكان.
  - عدم استخدام نسخ احتياطية قديمة بشكل آلي حتى يتضح الموقف مع مهندسي النظام تجنباً لتضارب البيانات.

### ب. توقف Gemini (عطل في نماذج الذكاء الاصطناعي)
- **الأثر:** فشل عمليات توليد النصوص، التحليل، والمساعدة في بناء السكريبتات.
- **الخطة:**
  - تعطيل التوليد (`FEATURE_AI_GENERATION = false`).
  - إيقاف الخصم من أرصدة المستخدمين (التأكد من أن حجز الرصيد يتم استرداده فور الفشل).
  - عرض رسالة واضحة للمستخدم: "خدمة الذكاء الاصطناعي معطلة مؤقتاً من المصدر، يمكنكم الاستمرار في كتابة المحتوى يدوياً".

### ج. توقف مزود الوسائط والفيديو (Video/Audio Providers)
- **الأثر:** فشل تصدير أو إنتاج الفيديوهات والصوتيات.
- **الخطة:**
  - تحويل وظائف الإنتاج لوضع "قيد الانتظار" (Queued) بحيث تُحفظ مشاريع المستخدم ولا يتم إعادة طلب التنفيذ (Retry) بشكل غير محدود لتجنب الفوترة المزدوجة، واعتماد نظام **Idempotency** لحفظ حالة الطلب.
  - تعطيل إنشاء تصديرات جديدة (`FEATURE_VIDEO_EXPORT = false`, `FEATURE_AUDIO_SYNTHESIS = false`).

### د. توقف خدمة الدفع (Google Play Billing / StoreKit)
- **الأثر:** تعطل شراء الأرصدة أو تجديد الاشتراكات.
- **الخطة:**
  - توفير فترة سماح (Grace Period) تبلغ 72 ساعة للمشتركين الذين حان وقت تجديدهم.
  - إيقاف الواجهة الخاصة بشراء الأرصدة لحين استقرار المتجر.

### هـ. توقف خدمة الإشعارات (FCM)
- **الأثر:** تأخر وصول تنبيهات اكتمال التصدير أو مراجعات الإدارة.
- **الخطة:**
  - الاعتماد على واجهة المستخدم (In-App Polling/Sockets) لتحديث الحالات الحرجة.
  - عدم الاعتماد الحصري على FCM لتبليغ قرارات الحذف أو التعليق.

### و. تعطل التخزين أو حذف ملف بالخطأ
- **الأثر:** اختفاء أصول مشاريع (Assets).
- **الخطة:**
  - استعادة الملف المحذوف فوراً عبر الـ Versioning في Cloud Storage.
  - في حال الفساد (Corruption)، يتم تحميل نسخة الـ Backup الأحدث.

---

## 4. وضع القراءة فقط (Read-Only Mode)
لضمان استمرارية الخدمة وتقليل الضرر المباشر أثناء انهيار بعض المخدمات (مثل تعطل خوادم التوليد والإنتاج):
- يتم تفعيل العلم `FEATURE_SYSTEM_READ_ONLY_MODE = true`.
- يتيح هذا الوضع للمستخدم قراءة القرآن، مشاهدة المشاريع المنتهية والمصدّرة، وتصفح المحتوى، لكنه يمنع الحفظ الجديد، الدفع، التوليد، أو إنتاج الفيديوهات.

---

## 5. اختبار التعافي (Recovery Testing)
- **سجل آخر اختبار:**
  - تم إجراء اختبار استعادة لمشروع محذوف جزئياً في بيئة الـ `Staging`. 
  - تمت محاكاة انقطاع Gemini واختبار تعافي الأرصدة المرجعة (Idempotency Check) (نجاح - انظر `DisasterRecoveryTest.kt`).

---

## 6. القواعد الصارمة للتعافي (Non-Negotiable Rules)
1. **لا تخفِ توقف الخدمة:** الإبلاغ بشفافية عبر قنوات التطبيق لمنع تكرار المحاولات.
2. **لا تعيد تشغيل عمليات مدفوعة بلا Idempotency:** إرفاق `transactionId` دائم لكل طلب.
3. **لا تفتح قواعد الوصول (Security Rules) أثناء الطوارئ:** الطوارئ ليست ذريعة لتجاوز حماية الـ Firestore/Storage.
4. **وثق كل قرار:** يجب على مدير الحادث توثيق توقيت وأسباب تفعيل أو تعطيل الخدمات في الـ Audit Log.
