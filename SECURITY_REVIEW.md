# تقرير المراجعة الأمنية (Security Review) - سراج

## ملخص تنفيذي
تم إجراء تدقيق أمني شامل لمشروع "سراج" قبل النشر، شمل فحص المستودع، وقواعد الأمان، ووظائف الخادم، وإدارة الأسرار. التطبيق يعتمد على بنية آمنة تمنع العميل من التحكم في الأرصدة أو الصلاحيات أو الوصول العشوائي للبيانات.

## جدول المخاطر والإصلاحات

| الخطر | المستوى | الملف/الخدمة المتأثرة | الإصلاح المطلوب | حالة الإصلاح |
|-------|---------|-----------------------|------------------|--------------|
| **تسريب الأسرار في الكود** | Blocker | `build.gradle.kts`, `google-services.json` | التأكد من استخدام قيم وهمية (dummy) في بيئة التطوير، وإضافة الملفات الحساسة لـ `.gitignore`. استخدام Secret Manager للمفاتيح الحقيقية. | ✅ تم التحقق (لا توجد أسرار حقيقية). |
| **تجاوز الصلاحيات (Role Escalation)** | Critical | `firestore.rules` (users) | منع العميل من تعديل حقول `role`, `balance`, `credits`, `isBanned` في وثيقة المستخدم الخاصة به. | ✅ تم التحقق (محمية في القواعد). |
| **إضافة مستخدم عشوائي لـ Workspace** | Major | `firestore.rules` (workspace_members) | كان يسمح لأي مستخدم بإضافة نفسه لأي مساحة عمل. تم تعديل القاعدة لتسمح فقط لـ OWNER/MANAGER بالإضافة. | ✅ تم الإصلاح عبر `patch_rules.py`. |
| **الرفع العشوائي للملفات (Storage)** | Major | `storage.rules` (workspaces) | كان يسمح لأي مستخدم مسجل بالرفع لأي مساحة عمل. تم قفل الرفع (write: false) ليتم عبر الخادم. | ✅ تم الإصلاح عبر `patch_rules.py`. |
| **التحايل على الأرصدة (Billing Bypass)** | Critical | `functions/src/index.ts` | التأكد من أن الخصم يتم في الخادم عبر Transaction قبل تنفيذ العملية وليس من التطبيق. | ✅ تم التحقق (`verifyAndChargeUser`). |
| **غياب التحقق من سلامة التطبيق** | Major | `SirajApplication.kt` | تفعيل Firebase App Check (PlayIntegrity) لمنع الطلبات من خارج التطبيق الرسمي. | ✅ تم الإصلاح وتفعيل App Check. |
| **تجاوز معدل الطلبات (Rate Limiting)** | Major | `functions/src/index.ts` | تطبيق حد زمني للطلبات لمنع استنزاف الرصيد أو مفاتيح الذكاء الاصطناعي بسرعة. | ✅ تم التحقق (5 ثوانٍ بين الطلبات). |
| **حقن النصوص وتجاوز القيود الشرعية** | Major | `functions/src/index.ts` (generateImage) | إضافة قائمة كلمات ممنوعة (Blacklist) للأنبياء والذات الإلهية على الخادم قبل إرسال الطلب. | ✅ تم التحقق (موجودة في الكود). |

## اختبارات تثبت الإصلاح
- محاولة تعديل `balance` من التطبيق سترجع خطأ `PERMISSION_DENIED` من Firestore.
- محاولة الوصول لخدمات Firebase من تطبيق معدّل (Tampered APK) سترفض بفضل `App Check`.
- استخدام كلمة "ملاك" أو "رسول" في توليد الصور سيرجع رسالة خطأ شرعية من الخادم قبل استهلاك الرصيد.

## خطة تدوير الأسرار (Secret Rotation Plan)
في حال تسرب أي من الأسرار في بيئة الإنتاج مستقبلاً:
1. **GEMINI_API_KEY**: يتم إبطاله من منصة Google Cloud Console وتوليد مفتاح جديد، ثم تحديث القيمة في Firebase Secret Manager.
2. **Firebase Admin Credentials**: إنشاء مفتاح جديد لـ Service Account وحذف القديم من IAM.
3. **App Store Connect Keys**: إبطال المفتاح من Apple Developer Portal وإنشاء واحد جديد وتحديث `.env` الخاص ببيئة الإنتاج.
4. **Android Keystore (Signing Key)**: لا يمكن تدويره بسهولة (سيؤدي لتغيير توقيع التطبيق في Google Play)، لذا يجب الاحتفاظ به في مكان آمن جداً. إذا تم تسريبه، يجب تفعيل خدمة "Play App Signing Key Upgrade" عبر دعم المطورين.

## الخلاصة
لا توجد مخاطر حرجة متبقية (Zero Known Blockers/Criticals). جميع العمليات الحساسة (دفع، صلاحيات، خصم أرصدة، اتصال بالنماذج) معزولة في واجهات الخادم (Backend) وقواعد الأمان (Security Rules). تمت الموافقة على القواعد الحالية.
