# دليل الوصول الشامل وتجربة الاستخدام الميسّرة (Accessibility Guide)

تطبيق **سراج** يلتزم بتمكين جميع المستخدمين باختلاف قدراتهم البصرية والسمعية والحركية والإدراكية من الاستفادة من ميزات المنصة ومحتواها الإسلامي وأدوات الإنتاج بسهولة ويسر وكرامة.

---

## 1. المعايير المعتمدة (Compliance Standards)

- **معيار WCAG 2.1**:
  - مطابقة معايير **المستوى AA** كأساس لجميع النصوص والعناصر التفاعلية (نسبة تباين >= 4.5:1 للنصوص العادية، و >= 3:1 للنصوص الكبيرة).
  - مطابقة معايير **المستوى AAA** في وضع التباين العالي (High Contrast Mode) بنسبة تباين تتجاوز **7:1**.
- **المعايير المادية للمس (Touch Targets)**:
  - الحد الأدنى لمساحة أي عنصر قابل للنقر هو **48×48 نقطة (dp)** وفق إرشادات Material Design 3 وإمكانية الوصول لنظام Android.

---

## 2. الرؤية والتباين العالي (Vision & High Contrast)

### 2.1 وضع التباين العالي (High Contrast Mode)
- **النمط الداكن الفائق (Dark High Contrast)**: خلفية سوداء نقية (`#000000`) مع نصوص وأيقونات بيضاء ساطعة (`#FFFFFF`) ولون تمييز ذهبي عالي الوضوح (`#FFD54F`) ومحددات حدود بيضاء واضحة.
- **النمط الفاتح الفائق (Light High Contrast)**: خلفية بيضاء نقية (`#FFFFFF`) مع نصوص سوداء نقية (`#000000`) ولون تمييز زمردي غامق (`#004D40`) وحدود تفصل بين البطاقات والحاويات.

### 2.2 تكبير الخطوط الديناميكي (Dynamic Typography Scaling)
- يدعم التطبيق 4 مستويات تكبير مباشرة من الإعدادات:
  - **100%**: الحجم القياسي المريح.
  - **115%**: حجم متوسط.
  - **130%**: حجم كبير.
  - **150%**: حجم كبير جداً للأشخاص الذين يعانون من ضعف حاد في الإبصار.
- تم ضبط ارتفاعات الأسطر (`lineHeight`) في `Type.kt` لتستوعب التشكيل وعلامات الضبط القرآني دون أي تداخل أو اقتطاع للحروف.

---

## 3. قارئات الشاشة واللغة العربية (TalkBack & Screen Readers)

- **اتجاه القراءة العربي (RTL Traversal)**:
  - تم استخدام `Modifier.sirajRtlTraversal()` و `traversalIndex` لضمان قراءة عناصر الشاشة من اليمين إلى اليسار ومن الأعلى إلى الأسفل منطقياً.
- **الأوصاف الدلالية (Semantics Labels & Roles)**:
  - كل زر وأيقونة وحقل إدخال يحمل وصفاً دلالياً عربياً واضحاً (مثل: `"إيقاف الصوت مؤقتاً"`, `"حذف المشهد"`, `"شريط التقدم: 45 بالمائة"`).
  - تم تحديد أدوار المكونات بدقة (`Role.Button`, `Role.RadioButton`, `Role.Switch`).
- **المناطق الحية (Live Regions)**:
  - تم ربط حالات التحميل والأخطاء ورسائل التحديث بمعدل `sirajLiveRegion` لإعلام المستخدمين بالتغييرات الحية تلقائياً دون الحاجة لإعادة فحص الشاشة.

---

## 4. الوسائط المتعددة الميسّرة (Accessible Media)

- **مشغل الفيديو (`SirajVideoPlayer`)**:
  - دعم الشروحات والترجمة النصية المصاحبة (`Closed Captions`) مع خلفية مظللة لضمان القراءة بوضوح.
  - أزرار تحكم قابلة للقراءة والتركيز الصوتي ومساحات لمس لا تقل عن 48dp.
  - إمكانية الإيقاف والتشغيل بسهولة دون تشغيل أصوات مفاجئة.
- **مشغل الصوتيات والتلاوات (`MiniPlayer`)**:
  - زر مباشر لقراءة **التفريغ النصي (Transcript)** للمقاطع الصوتية والمحاضرات والتلاوات مقسمة زمنياً.
  - إمكانية القفز في الصوت عبر النقر على الجملة المقروءة في التفريغ النصي.

---

## 5. تقليل الحركة والتفاعل اللمسي (Motion & Haptics)

- **تقليل الحركة (`Reduce Motion`)**:
  - تعطيل الانتقالات التفاعلية المتحركة والحركات المفرطة عند تفعيل الخيار للحد من الدوار والتشتت البصري.
- **التنبيهات اللمسية (`Haptic Alerts`)**:
  - تأكيد العمليات الهامة (الحفظ، الاعتماد، الحذف) باهتزازات لمسية خفيفة لعدم الاعتماد على المؤثرات الصوتية وحدها.

---

## 6. التحقق والاختبار (Verification & Automated Testing)

- تم إعداد فئة اختبارات شاملة `AccessibilityTest.kt` للتحقق من:
  - حساب نسب التباين ومعادلات WCAG.
  - تطابق لوحات الألوان مع المستوى AAA.
  - صحة عمليات التحجيم والتكبير للخطوط.
  - تكامل إعدادات المستخدم وتفضيلات الوصول.
