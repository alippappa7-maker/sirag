# سياق مشروع سراج (Project Context)

## معلومات أساسية
- **اسم المشروع:** سراج (Siraj).
- **المنصات المستهدفة:** Android (Kotlin/Compose) في المرحلة الحالية، مع هيكلية تدعم KMP مستقبلاً.
- **البيئة الحالية:** Development (تطوير).
- **التقنية المعتمدة:**
- Kotlin و Jetpack Compose.
- Clean Architecture (مقسم حسب الميزات Feature-First).
- Gradle (Kotlin DSL).

## آخر برومبت منفذ
034 (بناء قسم المحراب)
033 (مشغل الفيديو - Video Player)
032 (واجهة تصدير الفيديو والتنزيل والمشاركة - Export Video & Sharing)
031 (محرك تركيب وتصيير الفيديو السحابي - Video Composition Engine & Cloud Run Worker)
030 (طابور إنتاج الفيديو - Production Jobs & Cloud Tasks Queue)
029 (معاينة المشروع وفحص ما قبل التصدير - Project Preview & Pre-Export Validation)
028 (الترجمة النصية للفيديو - Subtitles & Timing)
027 (مكتبة الموسيقى والمؤثرات الصوتية - Soundtracks & SFX Library)
026 (الصوت الاصطناعي والتسجيل - Audio Studio & Voiceover)
025 (توليد الصور بالذكاء الاصطناعي - Imagen 3 والضوابط الشرعية)
024 (البحث في الوسائط الخارجية وإدارة التراخيص)
023 (مدير الوسائط)
022 (محرر المشهد)
021 (إنتاج المشاهد)
020 (إرسال المحتوى للمراجعة)
021 (إنتاج المشاهد)
019 (تكامل القرآن والتفسير)
018 (طبقة المصادر والتحقق الأولي)
017 (خطة المحتوى والسيناريو)
016 (تكامل Gemini الآمن)
015 (مولد الأفكار)
014 (قوالب المحتوى العربي)
013 (واجهة استوديو المحتوى)
012 (مساحات العمل والتعاون)
011 (نموذج بيانات المشاريع)

## المرحلة الحالية
- تم الانتهاء من تنفيذ PROMPT 031: محرك تركيب وتصيير الفيديو السحابي (Video Composition Service & Cloud Run/FFmpeg Engine).
  - إنشاء نموذج مخطط التركيب الكامل `VideoCompositionManifest` ومكونات المشاهد والانتقالات والصوت `CompositionSceneItem`, `CompositionAudioTrack`, `CompositionSubtitleItem`, `CompositionBranding`.
  - تطبيق واجهة ومستودع التركيب `VideoCompositionService` و `FirebaseVideoCompositionServiceImpl`:
    1. قراءة بيانات المشروع والمشاهد المعتمدة وترتيبها تصاعدياً حسب `orderIndex`.
    2. تطبيق الدمج الصوتي: التعليق الصوتي والتلاوة القرآنية كمسار رئيسي، والموسيقى/المؤثرات كمسار خلفي مع التلاشي.
    3. تطبيق الضوابط الشرعية الصارمة: عدم إضافة شارة "موثق" أو اقتباس المصدر إلا إذا كانت حالة مراجعة المشروع `APPROVED`.
    4. دعم المقاسات القياسية (9:16 للريلز، 1:1 مربع، 16:9 عريض) وخيارات الجودة (720p, 1080p, 4K).
    5. توليد نسخ المعاينة السريعة (Preview Clip) ونسخ الماستر النهائية (Full Production).
    6. حفظ وتخزين الناتج في Google Cloud Storage وتوليد روابط تحميل موقعة مؤقتة (Signed URLs) صالحة لمدة 7 أيام دون كشف الروابط الداخلية.
    7. استرداد الرصيد المحجوز آلياً عند أي فشل أو إلغاء للعملية.
  - إعداد خدمة المعالجة السحابية الخلفية `server/composition-worker/index.js` المبنية على Cloud Run و FFmpeg للتعامل مع طوابير Cloud Tasks بأمان وكفاءة ودون كشف أسرار.
- تم الانتهاء من تنفيذ PROMPT 030: بناء نظام طابور إنتاج الفيديو والمعالجة السحابية غير المتزامنة (Production Jobs & Cloud Tasks Queue).

  - إنشاء نموذج بيانات المهام `ProductionJob` وحالات المعالجة `ProductionJobStatus` (QUEUED, PROCESSING, RENDERING, UPLOADING, COMPLETED, FAILED, CANCELLED) وخيارات الجودة `ProductionQuality` (SD_720P, FHD_1080P, UHD_4K).
  - تطبيق مستودع المهام `ProductionJobRepository` و `FirebaseProductionJobRepositoryImpl`:
    1. التحقق من المشروع والمشاهد وحساب التكلفة السحابية وفق الجودة والمدة الزمنية دون الاعتماد على مدخلات العميل.
    2. تطبيق مبدأ منع التكرار (Idempotency Key) لمنع الخصم المزدوج أو إدراج مهام متطابقة في نفس الدقيقة.
    3. دعم الإلغاء الآمن للمهام قيد المعالجة واسترداد الرصيد المحجوز تلقائياً (`creditRefunded = true`).
    4. دعم إعادة المحاولة المحدودة (Limited Retry حتى 3 محاولات) مع تسجيل المحاولات في السجلات.
    5. تتبع حي ومستمر لحالة المهام عبر `Firestore SnapshotListener` والتدفقات `Flow`.
  - واجهة إدارة وتتبع مهام الإنتاج `ProductionJobsScreen` & `ProductionJobsViewModel`:
    - حوار بدء مهمة إنتاج جديدة يتيح اختيار جودة الفيديو، أبعاد الإطار، وتفعيل خيار حرق الترجمة داخل الفيديو مع عرض التكلفة المقدرة.
    - بطاقات تتبع مرئية لكل مهمة تظهر نسبة التقدم (Progress Bar)، حالة المهمة، تفاصيل الأبعاد والجودة.
    - قسم سجلات أحداث المهمة (Job Logs Bottom Sheet) لعرض أحداث المعالجة السحابية دون كشف أي مفاتيح أو أسرار.
    - إمكانية نسخ رابط التحميل المباشر والآمن للفيديو المكتمل فور جهوزيته.
    - ربط مباشر من شاشة معاينة المشروع `ProjectPreviewScreen` للبدء الفوري للتصدير بعد اجتياز فحص الجودة.
- تم الانتهاء من تنفيذ PROMPT 029: بناء مشغل معاينة المشروع الكامل وفحص ما قبل التصدير (Project Preview & Pre-Export Quality Gate).
  - إنشاء نماذج فحص الجودة والتصدير `PreExportValidationIssue`، `ValidationSeverity` (BLOCKER / WARNING)، `ValidationIssueType`، و `PreExportReport`.
  - تطبيق محرك فحص ما قبل التصدير الشامل (Pre-Export Quality Gate):
    1. **موانع حرجة (BLOCKERS):** مشاهد بدون مدة زمنية، تداخل أسطر الترجمة، أو مشروع خالٍ من المشاهد. (تمنع التصدير نهائياً).
    2. **تحذيرات وتنبيهات (WARNINGS):** مشاهد فارغة بدون وسائط أو تعليق، نصوص شرعية وآيات غير مربوطة بمصدر موثق في المحراب، أو أسطر ترجمة طويلة تتجاوز أبعاد الشاشة.
    3. إمكانية القفز المباشر من التقرير إلى المشهد أو محرر الترجمة المسبب للخطأ لإصلاحه فوراً.
  - واجهة مشغل المعاينة المتكاملة `ProjectPreviewScreen`:
    - دعم تبديل أبعاد الفيديو (9:16 طولي للريلز والتيك توك، 16:9 عريض، 1:1 مربع).
    - مسطرة توقيت شاملة (Scrubber) مع عداد للثواني وأجزاء الثانية.
    - عناصر تحكم متكاملة: تشغيل/إيقاف مؤقت، كتم/إلغاء كتم، تغيير سرعة العرض (0.5x, 1.0x, 1.5x, 2.0x)، والانتقال بين المشاهد السابقة والتالية.
    - شريط مصغرات المشاهد في الأسفل للانتقال الفوري لأي مشهد.
    - عرض الترجمة والشارات متزامنة مع توقيت المشهد الحالي.
    - شيت منبثق لتقرير التصدير مع إظهار حالة التصدير (مسموح / ممنوع).
  - ربط الشاشة في `AppNavigation` وبشاشة المشاهد `ScenesScreen`.
- تم الانتهاء من تنفيذ PROMPT 028: بناء نظام الترجمة النصية والشارات للفيديو (Subtitles & Timing System).
  - إنشاء نماذج البيانات `SubtitleItem`، `SubtitleStyle`، `SubtitleSourceType`، `SubtitlePosition`، `SubtitleFontFamily`، و `SubtitleReviewStatus`.
  - تطبيق الضوابط الشرعية والقانونية:
    1. قفل النصوص الشرعية (آيات وأحاديث موثقة) برمجياً لمنع إعادة صياغتها، مع السماح بضبط توقيتها فقط.
    2. عدم اعتبار الترجمة الإنجليزية تفسيراً أو نصاً مقدساً، ووضعها بحالة "بانتظار المراجعة" `PENDING_REVIEW` مع التنبيه بضرورة الاعتماد البشري.
    3. التحقق الصارم من التوقيت: منع تجاوز مدة المشهد ومنع التداخل غير المنطقي.
  - واجهة كاملة `SubtitleEditorScreen` تشمل:
    - شاشة معاينة فيديو تفاعلية تعرض الترجمة متزامنة مع مسطرة التوقيت (Scrubber).
    - توليد أسطر الترجمة وتوزيع التوقيت تلقائياً من نص المشهد/التعليق الصوتي.
    - تحرير أسطر الترجمة وتعديل البداية والنهاية.
    - نافذة تخصيص النمط: الموضع (أسفل، وسط، أعلى)، الخط العربي، حجم الخط، وخيار حرق الترجمة في الفيديو (Hardsub vs Softsub).
    - دعم تبديل اللغات بين العربية والإنجليزية وتوليد مسودات الترجمة.
    - محرك تصدير قياسي لملفات SRT و VTT.
  - ربط الشاشة في `AppNavigation` وبـ `SceneEditorScreen`.

- تم الانتهاء من تنفيذ PROMPT 027: بناء مكتبة الموسيقى والمؤثرات الصوتية (Soundtracks & SFX Library).
  - إنشاء نماذج البيانات `SoundtrackItem`، `SoundtrackCategory` (طبيعة وبيئة، مؤثرات SFX، وثائقي، إيماني، فوكالز بشرية، وموسيقى مرخصة)، و `SoundLicenseType`.
  - تطبيق سياسة الصوت الإسلامية:
    1. توفير فلتر إجباري يتيح إخفاء الموسيقى تماماً واستخدام أصوات طبيعية وفوكالز بشرية ومؤثرات SFX فقط.
    2. عدم فرض الموسيقى كعنصر إلزامي، واحترام خيار المستخدم في التعطيل.
    3. منع استخدام التلاوة القرآنية كخلفية صوتية تلقائية للمشاهد.
    4. الشفافية الكاملة في التراخيص وحقوق الاستخدام التجاري وقيود النشر ونصوص النسبة (Attribution).
  - بناء واجهة `SoundtrackLibraryScreen` متكاملة تشمل:
    - البحث والتصنيف والتصفية.
    - مشغل معاينة صوتية فوري.
    - نافذة ضبط طبقة الصوت للمشهد: التحكم بمستوى الصوت (Volume)، خيار التكرار (Looping)، وتلاشي الدخول والخروج (Fade-In / Fade-Out).
    - ربط مباشر للمؤثر أو الخلفية بالمشهد وتحديث طبقات الصوت في `Firestore`.
  - ربط الشاشة بمحرر المشهد `SceneEditorScreen` ومدير الوسائط `AssetLibraryScreen` و `AppNavigation`.

- تم الانتهاء من تنفيذ PROMPT 025: توليد الصور بالذكاء الاصطناعي (AI Image Generator) باستخدام Imagen 3.
  - إضافة واجهة كاملة `AiImageGeneratorScreen` تدعم وصف المشهد، نص الاستبعاد (Negative Prompt)، الأنماط الفنية الإسلامية (Islamic Art, Photorealistic, Cinematic, etc.)، ونسب العرض (16:9, 9:16, 1:1, 4:5).
  - تطبيق الفحص الشرعي الحازم في الواجهة والخادم لمنع تصوير وتجسيد الأنبياء والرسل والصحابة والملائكة والذات الإلهية، مع إظهار علامة "AI-GENERATED" وإخلاء المسؤولية "مولد بالذكاء الاصطناعي - لا يعتبر دليلاً شرعياً".
  - ربط التحقق من الرصيد والخصم الآمن (مع الاسترجاع عند الفشل أو الإلغاء) ونقاط نهاية Cloud Functions (`generateImage`, `cancelImageGeneration`, `deleteGeneratedImage`).
  - إمكانية تعيين الصور المولدة مباشرة كخلفية للمشهد أو حفظها في مكتبة وسائط المشروع.

- تم الانتهاء من تنفيذ PROMPT 024: البحث في الوسائط الخارجية والتحقق من التراخيص.
  - دعم مزودي المصادر الخارجية (Unsplash, Pexels, Pixabay, Wikimedia Commons, Internet Archive).
  - فحص التراخيص المفتوحة (CC0, Unsplash License, Pexels License, Public Domain) والتحقق من صلاحية الاستخدام التجاري والتعديل والنسب.
  - واجهة بحث متقدمة مع تنقية حسب النوع، الترخيص، الدقة، والأبعاد.
  - تخزين بيانات النسبة وعنوان المصدر وإثبات الترخيص تلقائيًا عند استيراد الأصل لمكتبة المشروع.

- تم الانتهاء من تنفيذ PROMPT 022: بناء محرر المشهد (Scene Editor).
- إنشاء شاشة مخصصة `SceneEditorScreen` تدعم التعديل الشامل للمشهد: العنوان، التعليق الصوتي، نص الشاشة، التنسيقات (لون، حجم، محاذاة، موضع)، والخلفية والانتقالات.
- دعم الحفظ التلقائي لكل تغيير وميزة التراجع والإعادة (Undo/Redo) داخل المحرر.
- دعم إضافة وتنسيق النص على الشاشة مع المعاينة الحية البسيطة.
- ميزة نسخ المشهد (Duplicate) مباشرة من داخل المحرر.
- تحذير وتطبيق القواعد الشرعية: إذا تم تعديل نص مشهد يحتوي على ادعاء موثق، يتم إرجاع المشهد إلى مسودة وتغيير حالة المشروع إلى `CHANGES_REQUESTED`.

## الميزات المكتملة
- المخطط المعماري الشامل.
- بنية Clean Architecture (core, domain, data, features).
- واجهات حالة التطبيق (شاشات التحميل، الخطأ، الفارغة، وعدم الاتصال).
- إعداد ملفات البيئة (Environment Config).
- **الهوية ونظام التصميم (Design System):** توحيد الألوان (كحلي، سماوي، ذهبي)، الخطوط وأحجامها (دعم العربية)، المسافات (Shapes)، ومكونات الواجهة الموحدة مع دعم الثيم الفاتح والداكن وحركات انتقال هادئة (Fade).
- **الهيكل الملاحي (Navigation):** إعداد مسارات التطبيق (Splash, Onboarding, Login, Tabs, Settings, Admin, Details)، مع بناء `MainShellScreen` لتوفير `BottomNavigation` للهواتف و `NavigationRail` للأجهزة اللوحية، وتجهيز نظام Deep Links وحارس مصادقة (Auth Guard) يحمي المسارات.
- **مكونات الواجهة والنماذج (UI Components & Mocks):** تم إنشاء `PreviewModels` ونماذج تجريبية مفصولة بوضوح عن مستودعات Firebase المستقبلية. كما تمت برمجة البطاقات (Project, Video, Audio, Flash, Source) ومؤشرات التحميل الهيكلية (Skeleton) وحالات التحقق والتقدم، ودمجها في الشاشات الرئيسية لاختبار التصميم.
- **المصادقة (Authentication):** طبقة `AuthRepository` متصلة بـ Firebase Auth للتعامل مع البريد/كلمة المرور وحماية مسارات التطبيق.

## الميزات قيد التنفيذ
- تم الانتهاء من تنفيذ PROMPT 011 (بناء النماذج وإنشاء طبقات التخزين لبيانات المشاريع).
- تم الانتهاء من الحزمة الأولى (الأساسيات، المصادقة، المشاريع، الإعدادات).

## الميزات المتبقية
- إعداد طبقة قاعدة البيانات المحلية (Room Database).
- التكامل مع خدمات Google Play للمصادقة عبر Google.
- إدارة الاشتراكات.
- إدارة مشاريع الفيديو والصوت.

## إعدادات يجب إجراؤها يدوياً (للمالك)
لربط مشروعك الحقيقي بـ Firebase واستخدام المصادقة:
1. اذهب إلى `console.firebase.google.com` وقم بإنشاء مشروع (مثال: `siraj-app`).
2. قم بتفعيل `Authentication` (اختر Email/Password و Google).
3. قم بتفعيل `Firestore Database` والصق القواعد الموجودة في ملف `firestore.rules`.
4. أضف تطبيق Android في إعدادات المشروع عبر كتابة الباكج الحالي `com.siraj.app`.
5. قم بتنزيل ملف `google-services.json` وضعه في مجلد `app/` داخل مساحة العمل الخاصة بك للحصول على مفاتيحك الخاصة (التطبيق حالياً يعمل ببيانات dummy لضمان نجاح البناء).

## المخاطر المعروفة
- الاعتماد على `google-services.json` وهمي قد يمنع عمليات تسجيل الدخول الفعلية لحين وضع الملف الحقيقي من قِبل المستخدم.

## الخطوة التالية المرجوة
- البدء بحزمة إنتاج المشاهد (Scene Production) والتعامل مع الصوتيات والمرئيات.