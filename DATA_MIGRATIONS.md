# نظام الترحيل وإصدارات المخطط (Schema Versioning & Data Migrations)
**منصة سراج (Siraj Platform)**

---

## 1. الهوية والهدف
مع تطور نماذج البيانات والـ Collections داخل النظام، يتطلب تحديث قواعد البيانات (Firestore) آلية ترحيل آمنة (Migration Engine) لا تعتمد على العميل (Client App)، وتضمن عدم فقدان البيانات القديمة أو كسر التوافق الخلفي.

---

## 2. النطاق والمجموعات (Collections)
نظام الترحيل يغطي ويراقب 13 مجموعة أساسية:
`users`, `workspaces`, `projects`, `scenes`, `assets`, `sources`, `reviews`, `flashes`, `audio`, `subscriptions`, `credits`, `notifications`, `auditLogs`.

الإصدار الحالي المستهدف في النظام: `SchemaVersion = 2`.

---

## 3. محرك الترحيل (Migration Engine)
المحرك مصمم للعمل خلفياً (يفضل عبر Cloud Functions/Admin Panel) ويقوم بالآتي:
- **اكتشاف الوثائق القديمة:** فحص `schemaVersion` داخل الوثائق.
- **التشغيل التجريبي (Dry Run):** يمكن تشغيل المحرك لمحاكاة الترحيل واكتشاف الأخطاء بدون حفظ أي تعديلات فعلية.
- **الترحيل على دفعات (Batching):** يتم الترحيل في دفعات (حجم افتراضي 100) لتجنب تخطي حدود Firestore وللحفاظ على الأداء.
- **إعادة المحاولة والتعافي (Retry & Backoff):** يحاول المحرك 3 مرات عند الفشل في جلب أو حفظ الدفعات قبل إعلان فشل المهمة (FAILED).

---

## 4. سياسات وقواعد الترحيل الآمن
1. **النسخ الاحتياطي (Backup Gate):**
   - يمنع المحرك بدء ترحيل فعلي (Non-Dry Run) دون التحقق المسبق من وجود نسخة احتياطية حديثة (Pre-Migration Backup).
2. **التوافق الخلفي (Backward Compatibility):**
   - **يُمنع** حذف الحقول القديمة فوراً. 
   - **يُمنع** تغيير معنى حقل موجود في نفس الإصدار، بل يُستحدث حقل جديد.
   - مثال: عند الانتقال من حقل `name` إلى `fullName`، يتم إنشاء `fullName` مع إبقاء `name` للمنصات القديمة.
3. **قفل التزامن (Concurrent Lock):**
   - يمنع تشغيل عمليتي ترحيل متزامنتين (Race Condition) لضمان عدم تلف البيانات.
4. **التتبع وإحصاء الفشل:**
   - يسجل المحرك عدد الوثائق الناجحة (Success) والفاشلة (Failure) مع حفظ أسباب الفشل لكل وثيقة `documentId` للرجوع إليها يدوياً.

---

## 5. خطة التعويض (Rollback & Compensation)
- في حال فشل الترحيل بنسبة خطيرة، يتم الاعتماد على النسخة الاحتياطية (Cloud Storage Backups) لاستعادة المجموعة كاملة. 
- يضمن وجود `schemaVersion` قدرة النظام على تشغيل سكريبت عكسي (Downgrade Migration) في حالات نادرة جداً.

---

## 6. اعتماد وثيقة ترحيل البيانات
- **المحرر:** مهندس الأنظمة والبنية التحتية - منصة سراج.
- **الحالة:** معتمد ونافذ برمجياً (PROMPT 093).
