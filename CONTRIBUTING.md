# دليل المساهمة والتطوير (Contributing)

نرحب بأي مساهمة في تطوير "سراج". لضمان جودة وتوافق الكود، يرجى الالتزام بالقواعد التالية:

## قواعد عامة
1. **الحفاظ على السياق**: لا تقم بأي تعديل كبير قبل قراءة `PROJECT_CONTEXT.md`.
2. **التحديث المستمر**: عند تنفيذ أي ميزة، يجب عليك تحديث `PROJECT_CONTEXT.md` وتوثيق التغييرات في `CHANGELOG.md`.
3. **لا للأسرار في الكود**: يُمنع منعًا باتًا وضع مفاتيح API، رموز الدخول (Tokens)، أو بيانات الاعتماد داخل ملفات الكود أو مستودع Git. استخدم الـ Secret Manager أو ملفات البيئة للحقن الآمن.

## القرارات المعمارية
- التطبيق يعتمد هيكلية `Kotlin Multiplatform`.
- الـ Views تعتمد على `Jetpack Compose`.
- المحتوى الشرعي (قرآن، أحاديث) يجب أن يفصل دائمًا ويوسم بطريقة تمنع تعديله العشوائي أو توليده آليًا بدون توثيق.
- أي قرار معماري جديد يجب أن يضاف ويوثق في `docs/DECISIONS.md`.

## خطوات تقديم إضافة (Prompt Workflow)
1. قراءة السياق وتأكيد الفهم.
2. بناء الميزة بشكل معزول واختبارها (Unit Tests للطبقات التي تدعم ذلك).
3. التأكد من البناء (`compile_applet` / `assembleDebug`).
4. تحديث سجلات التوثيق وإغلاق المهمة.
